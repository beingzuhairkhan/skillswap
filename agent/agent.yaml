server:
  log_level: info

metrics:
  global:
    scrape_interval: 15s

  configs:
    - name: skillswap
      scrape_configs:
        - job_name: "skillswap-metrics"
          metrics_path: /metrics
          scheme: https
          static_configs:
            - targets:
                - skillswap-e1yj.onrender.com